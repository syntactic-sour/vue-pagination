import { usePagination, DEFAULT_PAGINATION_LIMITS } from './composables/usePagination'
import { usePaginationWithRouter } from './composables/usePaginationWithRouter'

export default {
  DEFAULT_PAGINATION_LIMITS,
  usePagination,
  usePaginationWithRouter,
}
